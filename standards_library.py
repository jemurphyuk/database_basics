
import random
import math

print(random.randint(0, 1)) # example to print a random integer between 0 and 1 inclusive (i.e. heads or tails)

num_float = 23.26

print(math.ceil(num_float), math.floor(num_float))
